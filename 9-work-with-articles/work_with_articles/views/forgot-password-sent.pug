include header

.container
  h2 Посилання для відновлення надіслано
  p Якщо ви отримали токен — введіть його нижче, щоб перейти до відновлення пароля.

  form(action="/reset-password", method="POST")
    label(for="token") Токен:
    input(type="text", name="token", required)
    button(type="submit") Перейти до відновлення
