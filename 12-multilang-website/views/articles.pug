extends layout

block content
  h2 #{__('welcome')}

  if page
    .article-wrapper
      h2.article-title= page.caption[lang]
      div.art-text!= page.text[lang]
  else
    p.empty-message #{__('no_articles')}
